# "demo" is fully static. Use "realtime" only when the WebSocket backend is running.
VITE_APP_MODE=demo

VITE_REALTIME_WS_URL=

# Local development uses "/". GitHub Pages sets this to "/<repository-name>/".
VITE_BASE_PATH=/
